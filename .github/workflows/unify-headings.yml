name: Unify headings

# Disabled: this workflow rewrote HTML files after every push and made manual design fixes unreliable.
# It can still be started manually from GitHub Actions if ever needed.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Automatic heading rewrite is disabled."
