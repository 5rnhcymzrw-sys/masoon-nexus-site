name: Centralize header styles

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/centralize-header.yml

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test runner
        run: echo "runner ok"
